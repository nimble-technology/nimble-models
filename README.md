# Nimble Network Offline Models
This repo contains all the major AI models: wallet profiling, recommendation, user growth and so on.

## Install dependencies

```
pip3 install -r requirements.txt
```
